language: node_js

# Version number
node_js:
  - 0.10
  - 0.12
  - 4.2.3
 # - iojs-v1
  #- iojs-v2
  #- iojs-v3
  #- 5.0

# env:
#   global:
#     - FOO=Bar
#     - BAR=Foo

#in orderfail for 4.2.3 version
#matrix:
 # include:
   # - node_js: 4.2.3
  #    env: tt@!=rr
  #allow_failures:
   # - node_js: 4.2.3
      
build:
  ci:
    - node --version
   # - sleep 15m
    #- cs ps
    
integrations:
  notifications:
  
    - integrationName: ci-img1et
      type: webhook
      payload:
        - versionName=Shippable Run $BUILD_NUMBER 
      on_start: always
      on_success: always
      on_failure: always
      on_pull_request: always
      
   # - integrationName: ci-img2et
    #  type: webhook
     # payload:
      #  - versionName=Shippable Run $BUILD_NUMBER job
     # on_start: always
      #on_success: always
      #on_failure: always
      #on_pull_request: always 
     
   
